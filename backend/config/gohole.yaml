app:
  log_level: "debug"

http:
  address: ":8080"
  serve_frontend: true

dns:
  address: ":8053"
  upstream: "1.1.1.1"
  cache: true
  custom_domains: "example.com 10.10.10.10"
    - "test.com"

db:
  address: "192.168.0.3:9109"
  user: "gohole"
  password: "password"
  name: "default"
  debug: false

blocking:
  filter_strategy: "basic"
  blocklist_file: "block.txt"
  local_blocklist: "localblock.txt"
  local_allowlist: "localallow.txt"
